[package]
name = "testutil"
version = "0.1.0"
edition = "2021"

[dependencies]
assert_cmd = "2.1"
tempfile = "3.23"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
predicates = "3.1"
# For PathBuf and lazy loading usage
once_cell = "1.21"
